{
  "name": "tau-cloud",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "predev": "node scripts/pack-sequence.mjs",
    "prebuild": "node scripts/pack-sequence.mjs"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.2.0",
    "playwright": "^1.55.0",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@studio-freight/lenis": "1.0.39"
  }
}
